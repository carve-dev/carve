"""Stub command implementations for the carve CLI.

Each module exposes a `command` callable that typer registers as a subcommand
in `carve.cli.main`. For M1-01 these are stubs that print a TODO and exit 0,
except `init` and `version` which do real work.
"""
