"""Core runtime primitives for Carve.

This package houses the building blocks every other layer (CLI, server,
agent runtime) consumes. Anything that touches the filesystem, the state
store, or external services lives somewhere under `carve.core`.
"""
