# Carve — Delivery plan

**What to build, in what order, given what's already built.** This is the temporal layer ([`_strategy/2026-06-spec-structure.md`](./_strategy/2026-06-spec-structure.md)): it sequences work into dependency-ordered, foundation-first increments and carries the phase/increment identity (and the release tag at the end). The durable design lives elsewhere and is **not** organized by phase — [`PRD.md`](./PRD.md) (what/why/who), [`ARCHITECTURE.md`](./ARCHITECTURE.md) (the technical model), and the capability specs (under [`capabilities/`](./capabilities/); they describe *how a capability works*, version-independently).

This is a **living, delta-aware** document. It plans *changes and additions* to the current codebase, not greenfield builds. As increments land, update the *Current state* section and check off exit criteria; as priorities shift, re-sequence increments here — without touching the capability specs. It covers the **whole lifecycle** — the initial foundation build *and* ongoing change (bugs, enhancements, new capabilities) after it — under one structure: **Current state** (the perpetual delta baseline) → **Increments** (the initial build; becomes the build log) → **Backlog** (ongoing work that needs sequencing). How any individual change flows (the bug-vs-change rule, spec-first) is [`_strategy/2026-06-change-lifecycle.md`](./_strategy/2026-06-change-lifecycle.md).

> **Note.** Specs live in [`capabilities/<area>`](./capabilities/) (durable design); increments below reference them by capability name. `DELIVERY.md` — not `capabilities/README.md` — is the source of truth for sequencing and scope. The concrete **file manifest for each slice is not stored** — it is generated at build time (see *How a slice is built*, below).

---

## How to read an increment

Each increment is a shippable, dependency-respecting slice:

- **Goal** — the user-visible capability the increment delivers.
- **In scope** — the capability slices, each pointing at its design spec.
- **Depends on** — increments/code that must exist first.
- **Delta** — what's *new* vs. what *modifies* already-shipped code (the delta-aware part).
- **Exit criteria** — how we know it's done.

**How a slice is built.** The build (`/build-spec`'s planning stage) takes a slice = (capability spec × this increment) and **generates a *delivery spec*** at build time: it reads the spec as the design reference, inspects the **current codebase**, and emits the concrete, delta-aware file manifest (*create / modify*) plus the increment's slice of the spec's Acceptance + Tests as the bar. The manifest is computed, never stored — so it is always correct against what's already built (see [`_strategy/2026-06-spec-structure.md`](./_strategy/2026-06-spec-structure.md) → *The delivery spec*).

---

## Current state (the delta baseline)

Shipped and in `src/` — every increment plans *against* this:

- **M1 — walking skeleton.** CLI foundation (Typer), config loader, state store, the Anthropic agent loop, the Python step + `LocalVenvRunner` subprocess primitive, the Snowflake connector. The smallest end-to-end loop.
- **M1.1 — lifecycle + UX.** `carve init` templates, **API-key model auth** (`${ANTHROPIC_API_KEY}` via `models.toml`), dotenv autoload, plan progress, agent-prompt tightening, the **plan / build / run** separation, run-retry-permits-redo. (The plan/build/run separation is the shipped core of [plan-build](./capabilities/plan-build.md). The M1.1-02 *Claude-subscription OAuth* follow-up was **planned but never built** — only the API-key path of [model-auth](./capabilities/model-auth.md) shipped, with the Anthropic client constructed directly at four call sites; see Increment 1b.)
- **Spec 01 — state store → Postgres.** Landed (SQLite retired; Postgres baseline + the six audited migrations). Followups landed: the M1 test sweep and `DATABASE_URL` precedence. ~300 tests passing. **Increment 0 (state-store formalization) complete 2026-06-18** — spec reconciled to the shipped code.

- **Layout (Increment 1) — control-plane `carve.toml`.** Landed 2026-06-18: the `[components.<name>]` schema (transport-validated `url`/`ref`/`branch`), `ProjectPaths`, the component locator (`resolve_component` / `discover_components` / `workspace_dirname`), the git workspace cache (ref-pin, credential redaction, hardened env, bounded `timeout`), the provenance reader, and the `Workspace` model + migration `0007`. 879 tests passing.

- **Harness (Increment 1) — the Claude-Code-style agentic engine.** Landed 2026-06-19: sync sequential subagent `delegate` (`delegation.py`, `DelegationResult`, mode-clamp to `min(parent, capability)`, context isolation, harness-tracked `files_changed`); terminal-grade tools (`tools/fs_tools.py` edit/create_file with re-read-at-apply TOCTOU, `bash_tool.py`, `search_tools.py`, `web_tools.py`, `todo_tool.py`); the single pre-execution permission gate (`permissions/gate.py`, gate-first in `loop.py:_execute_tool_calls`, `grant ∩ mode` attenuation, fail-closed prompt) over the per-mode policy floor (`permissions/policy.py` with `_ALWAYS_DENY` + `DANGEROUS_BASH_FLAGS`) and the shared secret-path deny-list (`tools/secrets_denylist.py`); the bounded format-agnostic verification loop (`verification.py`); interrupt/cancel (`cancel.py`), steering (`steering.py`), and compaction (`compaction.py`). `loop.py` is a purely-additive MODIFY (sync preserved). Spec reconciled to the shipped code (minor inline drift only). Deferred follow-ups (non-blocking, in the harness spec's Open questions): grow the secret deny-list with the warehouse-cred surface, unicode-normalize the secret-name compare, and add dedicated `web_fetch`/`web_search`/`todo` unit tests.

- **Extensibility (Increment 1) — declarative agents, skill packs, hooks, MCP-consume.** Landed 2026-06-19: safe markdown-frontmatter agent loader (`agents/loader.py`, `yaml.safe_load`, `MAX_AGENT_FILE_BYTES=64 KiB`, fail-closed/no-partial), dispatch-time mtime-cached discovery with user-over-builtin override (`agents/discovery.py`), the classification router `select_agent` replacing the retired `AGENT_REGISTRY` (`agents/routing.py`), the advisory `max_mode` lint (`agents/lint.py`); skill packs as description-matched **content** via the `lookup_skill_pack` tool (`skills/packs.py`, `skills/pack_discovery.py`, added to the read-tools floor); `hooks.toml`-driven gated/mode-clamped/fail-closed hooks (`hooks/{config,events,runner,wiring}.py`) with `pre_tool`/`post_tool` live and `pre_deploy`/`post_build`/`on_run_failed` as wired-but-deferred-emitter seams; MCP-consume namespaced `mcp:<server>:<tool>` + effects-tagged + **fail-closed on missing effects** (`mcp/{config,client}.py`, `McpToolSpec` + `mcp:` prefix precondition + `mcp:<server>:*` wildcard grant expansion in `policy.py`); and the live-wiring seam (`cli/orchestrator/extensibility_wiring.py` — the `HookFactory = (mode) -> (pre, post)` that re-clamps a delegated child's hooks at `child_mode`, the skill-pack tool builder, and `resolve_agent_or_fallback`) plus the `carve agents`/`skills`/`mcp-servers` CLIs and the `hooks_file`/`mcp_file` `PathsConfig` keys. Security + agent-loop reviewers PASS; ruff/mypy --strict/1328 pytest green. Spec reconciled to the shipped code (minor inline drift only — the hook-factory seam and the `lookup_skill_pack` tool name).

- **Model auth (Increment 1b) — credential precedence + SDK-native OAuth.** Landed 2026-06-21: the single `client_factory.make_client` precedence resolver (explicit `auth_mode` wins and suppresses a stray opposite credential via the SDK header-omit sentinel; else `ANTHROPIC_API_KEY`; else a subscription OAuth bearer built with `auth_token=` + the `anthropic-beta: oauth-2025-04-20` header from `ANTHROPIC_AUTH_TOKEN`/`CLAUDE_CODE_OAUTH_TOKEN`; else a clear `ConfigError`; auto mode refuses a both-present env), collapsing the four prior `anthropic.Anthropic(api_key=…)` sites; `ModelsConfig` gained `auth_mode` (validated) + `tiers` + `resolve_model`; `default_model` → `claude-opus-4-8` with `pricing.py` updated to the current models; `carve auth status`/`login` (login wraps `claude setup-token`); per-agent model-tier resolution at delegation. Carve owns **no** browser flow or token store. Security reviewer PASS + an adversarial 27-cell precedence-matrix verification PASS (exactly-one-credential at the wire, OAuth always carries the beta header); ruff/mypy --strict/1367 pytest green. Deferred (non-blocking): thread `config.models.tiers` into the live delegation runner when the orchestrator constructs it.

- **Packaging (Increment 1) — bundled Postgres docker-compose + external-Postgres option.** Landed 2026-06-21: `carve init` renders a Postgres-only `docker-compose.yml` (`postgres:16`, slug-named container/volume so multiple projects don't collide, bound to `127.0.0.1` only, healthcheck) and adds a `DATABASE_URL` block to `.env.example`; `--external-postgres <url>` validates/normalizes the URL, skips the compose bundle, and migrates against the external DB directly (the URL is used as-is so a stray `DATABASE_URL` env can't redirect it). Migration is **graceful**: external is fatal-on-failure, but the bundled path defers with a next-step when Postgres isn't up yet (the real first-run case). Docker-absent + no `--external-postgres` → friendly exit 3. Idempotent (existing `docker-compose.yml`/`.env` left alone). Security note: the external path writes a **commented placeholder** to `.env.example` and prints the real password-bearing URL for the user to paste into the gitignored `.env` — never to a committed file (a leak the adversarial review caught and that's now regression-tested). Helpers in `cli/commands/packaging.py`; 3 docs added. ruff/mypy --strict/1397 pytest green.

- **Init (Increment 2, lean) — the control-plane `carve init` rewrite.** Landed 2026-06-22: the `carve.init` package (`detect` → `resolve` → `scaffold`) and a thin rewritten `carve init` command. **Detection**: dbt at root + one level down, dlt via `.dlt/` or AST-parsed `import dlt` (parse, never exec), git, docker (`shutil.which`), re-init. **Resolution** (`plan.resolve`): non-interactive across the Postgres × dbt × dlt axes, precedence explicit-flag > detected > default, with clean `InitError`s on conflicting flags, ambiguous multi-detect, and dbt/dlt names colliding. **Scaffold**: the control-plane `carve.toml` renderer (simple mode writes **no** `[components.*]` block — same-repo dbt/dlt is convention-discovered; separate-local/-remote get one; no `[state_store]` block, so it loads before `.env` exists), empty memory templates (`standards.md`/`decisions.md`/`conventions.md`), `.env.example` + `.gitignore`, bundled compose (or external-Postgres placeholder with the real URL printed, never committed), `--with-dbt`/`--with-dlt` greenfield scaffolds; idempotent (skip-if-exists, symlink-safe) and re-init-preserving. Then default-target wiring + graceful state-store migration (external fatal, bundled defers) + `git init`. Adversarial correctness+security review (2 BLOCKER + 2 MAJOR + 1 MINOR) fixed and regression-tested: non-BMP `carve.toml` escaping, duplicate-component-name guard, SCP-URL component naming, up-front `--default-target` validation (exit 2, no half-write), dangling-symlink write-through. ruff/mypy --strict/1439 pytest green. **First-run cleanup pass (2026-06-22)** — a completeness assessment found the lean init printed a quickstart that didn't run; fixed: the default-target `[snowflake.<name>]` is now scaffolded **commented** in `connections.toml` so a fresh project loads without warehouse creds (the advertised `carve plan` was dying at config load on unset `${DEV_SNOWFLAKE_*}`); `conventions.md` is now comment-only and the EL agent skips comment-only convention docs (it was being told "no conventions inferred"); printed Next-steps drop the `carve serve` stub; init prints a brownfield-detected acknowledgement; `--destination-kind` (dead flag) cut; spec drift reconciled (exit-2 convention, `carve.toml` shape, file inventory, skip-only re-init). 1441 pytest green. **Deferred** (each tracked, issues #9–#14): convention *inference* (placeholder `conventions.md`), interactive prompts (`--non-interactive` accepted; resolution is always non-interactive), `--migrate-from-targets`, dbt-engine detection/eager-install, auth-token bootstrap, getting-started docs.

- **Memory (Increment 2, lean) — the runtime read/append machinery for project memory.** Landed 2026-06-22: the `carve.core.memory` package. **`MemoryLoader`** — mtime-cached reads of the five file types (`conventions`/`standards`/`decisions` at `carve_dir/*.md`; `pipelines/<name>.md`; `el/<name>/NOTES.md`) over `ProjectPaths`, `None` for absent files, `invalidate()`. **`select_for_task`/`MemoryBundle`** — always conventions+standards; decisions only when `is_investigative`; sidecars when present. **`MemoryWriter.append_decision`** — the one write exempt from the plan/build gate; newest-first anchored to the dated-entry region (not the scaffolded `## Format` docs), dup-by-(title,date) → `DecisionAlreadyExists`, single-line-title guard (no forged headings), atomic temp+`os.replace` write, loader invalidation. **`carve memory show / edit / append-decision`** CLI (edit writes directly + unlinks an abandoned-empty sidecar). **Dormant** orchestrator hook `attach_memory_to_context` (unit-tested; no caller produces a goal classification until plan-build). Adversarial review (2 lenses + verify): 2 MAJOR + several MINOR fixed and regression-tested — the newest-first-vs-`## Format` insert bug, the title-newline forge, the abandoned-empty-sidecar, non-atomic writes. Spec drift reconciled (decision-entry format is freeform-body, not enforced labels). ruff/mypy --strict/1471 pytest green. **Deferred** (each tracked): `carve memory refresh` (needs the convention-inference engine — same blocker as init #9), REST `/api/v1/memory/*` + MCP parity (rest-api/mcp — later increment), the `plan_id`-gated `standards`/sidecar writes (state model can't express the gate), and the live orchestrator wiring (goal classification lands with plan-build).

- **SQL (Increment 2, lean) — a dialect-aware tool layer + DuckDB substrate.** Landed 2026-06-22: the `carve.core.sql` package. A **`sqlglot` classifier** (read/write/DDL/destructive, fail-closed) that catches what the old regex missed — `WITH…INSERT`, `SELECT…INTO`, multi-statement, trailing-comment nodes; **`validate`/`transpile`/`normalize_dialect`** (snowflake+duckdb first-class, postgres/bigquery/databricks/tsql author-only); dialect-dispatched **`introspect`** (`INFORMATION_SCHEMA`, caps + `truncated`, DuckDB scoped to one catalog); and the **`sql` tool** (ops `validate`/`transpile`/`introspect`/`run`) bound to a connection's dialect + the active `PermissionMode`, with write/DDL enforced in-tool via the shipped `warehouse_roles` floor (**deploy-only**, destructive-DDL approval). A first-class **DuckDB connector** (local/test substrate — the whole stack runs creds-free) + a `[duckdb.<target>]` connection type; `sql` registered in the permission policy; a **dormant** `sql-specialist.md` builtin. Adversarial review (2 lenses + verify) caught a **BLOCKER** (`SELECT…INTO` classified READ → a write on the read role in read_only) and a **MAJOR** (DuckDB introspection double-counting attached catalogs); both fixed + regression-tested, along with trailing-comment and exact-cap-truncation minors. Spec reconciled (deploy-only writes; op-set `validate/transpile/introspect/run`; the catalog-skill/`run_snowflake_query` generalization explicitly deferred — M1 paths untouched). ruff/mypy --strict/1533 pytest green. **Deferred** (each tracked): the catalog-skill + `run_snowflake_query` generalization, first-class introspection for the four author-only dialects, the orchestrator wiring of the specialist, and the `sql` step type (Increment 3).

- **Tool binder (Increment 3, foundational) — declarative grants → real executors.** Landed 2026-06-23: the harness seam that makes any declarative subagent able to *run* its tools. `subagent_registry.grant_stub_tool` had been yielding name-only stubs whose executor **raised** — so no declarative agent (including the just-shipped `sql-specialist`) could execute its `tools:` grant. New `carve.core.agents.tool_binding.bind_grant_tools(declared_tools, BindingContext)` maps each grant name to the real harness base tool (`read_file`/`grep`/`glob`/`edit`/`create_file`/`bash`/`web_fetch`/`web_search`/`todo`) from `(project_dir, child-clamped gate, approver)`; names whose dependency the harness doesn't hold (`sql`/`run_snowflake_query`/`mcp:*`/`dlt_*`) are supplied by the caller via `extra_tools` (with a bound-name == grant-name precondition), else stay a raising stub (fail-loud). Only grant **stubs** are bound — a spec/fixture's real tools pass through untouched (a `is_grant_stub` marker distinguishes). `SubagentRunner` builds the gate from the grant then binds at `child_mode`, so a delegated agent's `bash`/`edit` run clamped to its (narrower) authority. This retroactively makes the `sql-specialist` functional (its `sql` grant binds when the orchestrator injects a connection-backed `sql` tool) and unblocks every Increment-3 engineer. Adversarial review (2 lenses): the one real finding (a name-mismatched injected tool failing silently) fixed → fail-loud + regression-tested; a pre-existing loop duplicate-tool-name gap filed (#26). ruff/mypy --strict/1542 pytest green. **Deferred** (the live orchestrator wiring that constructs `SubagentRunner` with `extra_tools` + a goal classification remains, as before).

- **DLT-engineer phase 1 — substrate (Increment 3).** Landed 2026-06-23: the `carve.integrations.dlt` package the engineer runs on. `dlt` pinned as a dependency. **`verify.parse_dlt_run`** turns a `dlt pipeline run` into the verification loop's `CheckResult` by reading dlt's on-disk **load package** (`load/{loaded,normalized,new}/<id>/`) — user tables (internal `_dlt_*` filtered), applied schema changes, and failed jobs — exit-code-gated, with the real error line surfaced from stdout on failure (the harness `run_check` routes output there). **`code_emitter`** writes the Carve provenance header that the existing reader round-trips. Adversarial review ran *real* failing dlt loads and confirmed the false-green invariants hold (terminal/partial job failures, stale-package masking, malformed artifacts all caught); fixed the one MAJOR (error tail read the wrong stream) + hardened load-id ordering to numeric; a provenance-reader round-trip gap filed (#28). 16 substrate tests incl. a real DuckDB run; ruff/mypy --strict/1558 pytest green. **This is phase 1 of building the dlt-engineer toward its full spec** — phases 2 (the four callable skills + `sources/` corpus), 3 (the agent with all four strategies + the author→run→verify vertical), and 4 (the reviewers + the harness review fan-out) follow; only live goal-routing is genuinely blocked (the plan-build classifier).
- **DLT-engineer — first callable skills (Increment 3).** Landed 2026-06-23 (PR #32): the engineer's first two domain skills as lightweight callable **Tools** bound through the binder's `extra_tools` seam (NOT warehouse-coupled `@skill` functions). **`existing_dlt_inspect`** lists `el/` components by provenance (carve-generated vs user-authored) and reads a component's `__init__.py`+`requirements.txt`, path-confined to `el/<name>/`. **`rest_api_explore`** does a bounded read-only probe (OPTIONS `/` + openapi/swagger discovery + sampled GETs), request-capped, body-truncated, GET/OPTIONS only, every request returned for audit, fetcher injected so tests run offline. Spec-first: `dlt-engineer.md` + `extensibility.md` now record that project-tree/network domain skills are callable Tools while warehouse catalog skills stay `@skill`. ruff/mypy --strict/1569 pytest green.
- **DLT-engineer — library skills + reference connector (Increment 3).** Landed 2026-06-23: the next three deliverables on the same callable-Tool substrate, all gates green (ruff/mypy --strict/1592 pytest), all three reviewers (qa/security/python) PASS. **`dbt_source_lookup`** is a callable Tool (`integrations/dbt/sources.py`, `make_dbt_source_lookup_tool`) with `op=list` (`dbt_sources_list`) / `op=match` (`dbt_source_match`), reading dbt `sources.yml` via `yaml.safe_load`, schema-defaults-to-name; dbt-project resolution is the shared `integrations/component_locator._detect_dbt_project` (root + one-level-down) — there is no `integrations/dbt/locator.py` (spec reconciled inline). **`sources/_reference_hackernews`** is the one reference curated source pack (valid `SKILL.md` + inert bundled dlt source + pinned `requirements.txt` + introspection stub). **`dlt_library`** is a callable Tool (`integrations/dlt/library.py`, `make_dlt_library_tool`) with `list`/`lookup`/`copy` over `src/carve/sources/`, built on the shipped `pack_discovery.SkillPackLibrary` + `code_emitter` provenance, `copy` path-confined to `el/**`.
- **DLT-engineer — the agent + strategy packs + verification runner (Increment 3).** Landed 2026-06-23: the declarative agent itself plus its strategy guidance and the runner bridge, gates green at **1611 pytest**, all reviewers PASS after **one fix iteration** that corrected the verification-loop mechanism spec-first. **`core/agents/builtin/dlt-engineer.md`** — the declarative agent (frontmatter: `max_mode: build`, `tools:` flat-list grant, `allowed_paths` `el/**` + `.dlt/*.template`, 5 classifications; `model:` **omitted** → `default_model`; an 11-section system-prompt body encoding the corrected verification-loop). **`core/skills/builtin/dlt_strategies/{curated_library,rest_api_config,native_dlt,singer_wrapper}/SKILL.md`** — the 4 authoring-strategy content packs (loaded + matched on keyword). **`integrations/dlt/runner.py`** — the parse→`CheckResult` bridge (`make_dlt_parse_fn` / `run_dlt_check` / `make_dlt_verification_loop`) + `dlt_inspect_command` (read-only `dlt pipeline <name> info` helper); the single-execution-path invariant is preserved (the runner only *parses*; every command rides the gated `bash` tool). **Spec-first correction (the one fix iteration):** the agent executes the component via Carve's **venv runner** (`LocalVenvRunner`), NOT a nonexistent `dlt pipeline run`/`check` CLI and NOT gate-denied freeform `python`; the verdict comes from the parsed on-disk load package; `dlt pipeline <name> info`/`trace` via bash is inspection-only — this was reconciled into `dlt-engineer.md` §"The verification loop" + ~8 related references. Spec reconciled (the frontmatter example now tracks the shipped file — `model:` omitted, `max_mode`/`create_file` added; the deferred orchestrator-wiring items recorded in Open questions).
- **DLT-engineer — the reviewers + the review fan-out driver (Increment 3).** Landed 2026-06-23: the two read-only review subagents plus the orchestrator-owned fan-out driver, gates green at **1631 pytest**, all 4 reviewers PASS after **one trivial fix iteration** (a test type-ignore + an `__all__` export). **`core/agents/builtin/dlt-qa.md`** — the correctness/quality reviewer (`max_mode: read_only`, `tools: [grep, glob, sql, read_file]`, `allowed_paths: []`, `model:` omitted): schema-contract fit via `sql` introspect, incremental-cursor/idempotency/write-disposition sanity, requirements pinning, provenance-header presence, convention adherence; reports structured findings, never edits. **`core/agents/builtin/dlt-security.md`** — the safety reviewer (`max_mode: read_only`, `tools: [grep, glob, read_file]` — **no `sql`**, no warehouse): live-credential/secret leaks, data-loss-risky write dispositions, `rest_api_explore` bounds; reports, never edits. **`core/agents/review_fan_out.py`** — the orchestrator-owned driver as a **wired-but-dormant seam**: a `Finding`/`Severity`/`ReviewResult` model + `review_fan_out(diff, goal, delegate_fn) -> ReviewResult` that sequences dlt-qa→dlt-security over an **injected** `delegate_fn`, passes only `{diff, goal}` (never the engineer's transcript), parses findings **fail-loud**, stamps `reviewer` from the trusted call site, and sets `passed` = no `blocker`/`major`. Live orchestrator goal-routing that constructs the real `delegate_fn` is deferred (blocked on the plan-build classifier). Tests: `tests/core/agents/builtin/test_dlt_reviewers.py`, `tests/core/agents/test_review_fan_out.py` (stub delegate). Spec reconciled inline (the reviewers carry `max_mode: read_only` specifically, not an interchangeable "plan" mode; the deferred live-wiring recorded in Open questions item (e)). **Remaining dlt-engineer unit, in order** (resumable via `/build-spec dlt-engineer`): retiring the M1 `extract_load` agent. **Deferred orchestrator-wiring (cross-cutting, blocked on the plan-build classifier):** register `dlt_strategies/` as a skill-pack discovery root; inject the `sql`/`dlt_library`/`rest_api_explore`/`dbt_source_lookup`/`existing_dlt_inspect` grants via the binder's `extra_tools` + append `lookup_skill_pack` to the delegated tool set; thread `allowed_paths` through `delegation.py`'s `AgentPolicy` with a glob-aware write-path matcher (advisory today — a pre-existing harness gap); wire the live venv-runner→agent execution path (the runner bridge exists; the live wiring is deferred); wire the live review fan-out (the driver + reviewers + findings model ship and are unit-tested with a stub delegate; the orchestrator constructing `delegate_fn` over `SubagentRunner.run` + binding dlt-qa's `sql` grant via `extra_tools` + the live engineer↔reviewer fix loop is deferred).

- **dbt-engineer — substrate (Increment 3).** Landed 2026-06-23: the first slice of the dbt-engineer capability, built **in units like the dlt-engineer** (the manifest reader + the verify bridge), all gates green at **1662 pytest**, all 4 reviewers (qa/dbt/python/security) PASS. **`integrations/dbt/manifest.py`** — the `dbt_manifest` callable Tool (op-dispatch `list_models`/`model_columns`/`model_dependencies`/`tests_on_model`) reading dbt's real compiled `target/manifest.json` (`nodes`/`sources`/`parent_map`/`child_map`, no invented fields), on the **shipped** `component_locator._detect_dbt_project` locator (root + one-level-down — never a new locator); path-confined, with an offline-test seam (`paths`/`dbt_root`/`target_path` — exactly one), missing-project → empty/not-found, malformed manifest → fail-closed `ToolExecutionError`. This is the `dbt_manifest` skill family the capability spec describes in §"The subagent". **`integrations/dbt/verify.py`** — `parse_dbt_run` turning a finished `dbt build`/`test` into a harness `CheckResult` by reading the on-disk `run_results.json` (per-node `success`/`pass`/`skipped` vs `fail`/`error`), **exit-code-gated, artifact-trusted, fail-closed** (a clean exit with no readable artifact is *not* trusted green — the same false-green invariant as `dlt.verify.parse_dlt_run`); manifest-assisted readable node names. **`integrations/dbt/runner.py`** — `make_dbt_parse_fn` binding the artifact paths into the harness `verification.ParseFn` contract, plus thin `run_dbt_check`/`make_dbt_verification_loop` wrappers; **single-execution-path invariant preserved** (the runner only *parses*; every command rides the gated `bash` tool) — the LIVE `dbt build`/`test` execution is **deferred to dbt-execution**, documented exactly like `dlt/runner.py`. `__init__.py` exports updated; tests + fixtures under `tests/integrations/dbt/`. Capability spec sanity-checked, left as-is (phase-free design doc; the named skill family + verify-by-execution flow already match the substrate — no drift). **PARTIAL dependency picture — remaining dbt-engineer units** (resumable via `/build-spec dbt-engineer`): the declarative `dbt-engineer.md` agent (classifications + the author→run→verify vertical), brownfield convention-inference / greenfield scaffolding, and the `dbt-qa` reviewer (reusing the shipped `review_fan_out.py` driver + findings model). **The LIVE verify-by-execution loop is gated on `dbt-execution`** (net-new, **unbuilt** — `/build-spec dbt-execution`): the runner bridge parses, but the live `dbt build`/`test` command it rides doesn't exist yet. **Cross-cutting blockers (shared with the dlt-engineer's deferred wiring):** `dbt-execution` + the live orchestrator wiring (goal classification from plan-build + the venv/backend execution path + the live review fan-out). **Deferred reviewer polish (next dbt-engineer unit, non-blocking):** document the `warn`-status → `passed=False` choice (today `warn` is *not* in `_OK_STATUSES`, so a `--warn-error`-style warn fails the run — make that intentional + explicit); and surface the `run_results.json` per-node `failures` count in the failure message (today `_failure_result` reports the node id + dbt's `message`, not the numeric `failures`).

- **dbt-engineer — the agent + author→run→verify vertical + dbt-qa reviewer (Increment 3).** Landed 2026-06-24: the declarative dbt-authoring agent, its verify-by-execution vertical riding the now-shipped `LocalDbtBackend`, and the dbt-qa reviewer — built **in units like the dlt-engineer**, all gates green (ruff/mypy --strict) at **1717 pytest** (5 skipped, 0 failed), adversarial multi-agent review CLEAN after **one fix iteration**. **`core/agents/builtin/dbt-engineer.md`** — the declarative build-mode agent (frontmatter: `tools: [edit, create_file, grep, glob, sql, dbt_manifest]`, `allowed_paths` `models/**`/`tests/**`/`snapshots/**`/`**/*_schema.yml`/`sources.yml`/`dbt_project.yml`, `max_mode: build`, 5 classifications `new_model`/`modify_model`/`add_tests`/`declare_source`/`refactor_models`; `model:` **omitted** → `default_model`). It **verifies by execution THROUGH dbt-execution's `LocalDbtBackend`** — no freeform `bash` in its grant, no shelled `dbt` CLI; the dbt run rides the structured, secret-stripped backend the runtime injects. **`core/agents/builtin/dbt-qa.md`** — the read-only reviewer (`max_mode: read_only`, `tools: [grep, glob, sql, read_file, dbt_manifest]`, `allowed_paths: []`): coverage / convention / SQL-quality lenses, structured `findings` matching `review_fan_out.Finding`; reports, never edits. **`core/dbt_execution/verify_bridge.py`** — `dbt_run_result_to_check_result` (the `DbtRunResult`→`CheckResult` adapter, fail-closed verdict inherited) + `DbtBackendVerificationLoop` / `make_dbt_backend_verification_loop` (the structured author→run→verify loop, mirroring `VerificationLoop` semantics — the backend owns its subprocess, **no second exec path**); this is now the **preferred** verify path (the bash-shelled `integrations/dbt/runner.make_dbt_verification_loop` stays valid for recovery/legacy). MODIFY **`core/agents/review_fan_out.py`** — generalized with an optional `reviewers` param (default preserves the dlt pair), exporting `DBT_QA_REVIEWER` / `DBT_REVIEWER_SEQUENCE` (`("dbt-qa",)`). MODIFY **`integrations/dbt/verify.py`** — `_error_summary` promoted to public `error_summary` so the bridge reuses it for dbt-log diagnostic parity. Tests: `tests/core/agents/builtin/{test_dbt_engineer_agent,test_dbt_qa_reviewer}.py`, `tests/core/dbt_execution/{test_verify_bridge,test_verify_vertical,test_verify_vertical_real_dbt}.py`, `tests/core/agents/test_review_fan_out.py` (extended). **The one fix iteration** restored dbt-log diagnostic parity with `parse_dbt_run` (the bridge now surfaces the dbt log tail + status-accurate summaries on the non-green / `error` paths, not a static line) and fixed an `importorskip` placement that had been silently skipping the always-on injected-backend vertical test. Capability spec sanity-checked; **no drift** — the named agent + dbt-qa + verify-by-execution-through-dbt-execution already match the phase-free design body (left as-is). **Remaining dbt-engineer unit** (resumable via `/build-spec dbt-engineer`): the **brownfield convention-INFERENCE engine + greenfield project scaffolding** (deferred from this unit; the agent authors *in* the inferred style today, but the inference engine itself is a later unit — same convention-inference blocker as init #9 / memory `refresh`). **Deferred live wiring (cross-cutting, blocked on the plan-build classifier):** the orchestrator injecting `LocalDbtBackend` + the `dbt_manifest`/`sql` grants via the binder's `extra_tools` + live goal-routing to the dbt-engineer / dbt-qa; and the live review fan-out via `review_fan_out` with a real `delegate_fn`. **dbt-execution backend follow-up:** add `dbt source freshness` to the backend command set (`DbtCommand`) so the agent's authored source-freshness tests are actually *verifiable* — today the agent authors freshness but the backend command set can't run it (normal dbt workflow, flagged by review). **Observability note (Increment 4):** the verify `CheckResult.details` now carries a `logs_tail` (dbt stdout/stderr tail) for self-correction; IF/when `CheckResult.details` is persisted or telemetered, ensure **log scrubbing** — the dbt subprocess env carries warehouse creds (the `Subprocess` env-strip is an `ANTHROPIC_*` denylist, not an allowlist), so a dbt error could echo a cred into the tail. Latent today (the tail is consumed only by the agent context, at parity with the already-accepted `parse_dbt_run` output_tail); it matters at persistence / telemetry time.

- **dbt-execution — local-backend slice (Increment 3).** Landed 2026-06-23: the **first slice** of the dbt-execution capability (a *Drafting* spec) — the backend-agnostic step interface + the `local` dbt-core subprocess backend + engine resolve/pin — riding the shipped dbt substrate, all gates green at **1691 pytest**, all 4 reviewers (qa/dbt/python/security) PASS. New `carve.core.dbt_execution/`: **`result.py`** (`DbtRunResult`/`PerModelResult` — the single backend-uniform normalization seam, built from the **shipped** `integrations/dbt/verify.read_run_results`, **no re-parse** of `run_results.json`; fail-closed verdict inherited — a clean exit with no readable artifact is **not** green); **`backend.py`** (the `DbtBackend` Protocol + the typed `DbtCommand` value object with `to_argv` — arg-flattening to dbt argv in one tested place, option-shaped-selector/target guards, the `--full-refresh`-on-`test`/`snapshot` guard); **`local.py`** (`LocalDbtBackend` — **subprocess-always** (never the in-process `dbtRunner`, the concurrency-correctness requirement), the **injected** engine binary path (so connect's lazy *install* stays genuinely deferred and tests run with a fake engine), secret-env-stripping + process-group discipline, `UnsupportedBackendError` raised at *backend construction* for the deferred backends so a config naming one still round-trips/loads); **`engine.py`** (`resolve_engine`/`resolve_or_reuse`/`pin_engine` — Fusion-vs-dbt-core **by warehouse dialect** with the "pinned-and-reused-not-re-resolved" invariant + a `tomlkit` comment-preserving pin write-back into `[components.<name>]`, and the **Apache-2.0-dbt-Core-v2.0-not-ELv2-Fusion** license guardrail enforced in code/comments). New shared **`core/runners/subprocess.py`** — the subprocess/process-group/secret-strip primitive **factored out of** `LocalVenvRunner` (single source of truth; venv-runner behavior unchanged). MODIFY `core/config/schema.py` — the dbt `ComponentConfig` backend-selecting fields (`dbt_backend`/`dbt_engine`/`dbt_version`/`dbt_env`/`dbt_path`/`profiles_dir`/`worker_label`; `worker_label` is **accept-and-store-only** pending runtime placement; deferred backends accepted by the validator, rejected at construction). MODIFY `integrations/dbt/verify.py` — **additive, backward-compatible** enrichment (`DbtNodeResult` + a defaulted `DbtRunReport.nodes`) so `read_run_results` surfaces per-model status + `failures` **without re-parsing** (existing tests pass). `pyproject.toml` — a new `[project.optional-dependencies] dbt-test` group (dbt-core/dbt-duckdb), **NOT** in `dev`. Tests under `tests/core/dbt_execution/` (an injected-engine offline unit layer + an `importorskip`-gated real-dbt-DuckDB integration test). Capability spec sanity-checked; **one MINOR inline fix** (the interface sketch now tracks the shipped `DbtCommand` value object + `duration_ms` field) — otherwise the design body (two families, engine resolve/pin, license guardrail, subprocess-always, the component-config block) already matches. **This is the key unblock for dbt-engineer's verify-by-execution loop:** its `runner.py` parse-bridge now has a *runnable* local dbt backend — the live `dbt build`/`test` command its `ParseFn` waited on exists. **Still deferred/gated this slice:** the **managed** backends (`snowflake-native`/`dbt-cloud`/`remote` — round-trip in config, raise `UnsupportedBackendError` on run), **worker placement** (`worker_label` stored only — the labeling/routing is runtime, Increment 4), **lazy engine install** (resolve/pin land here; the install populating the injected engine path is connect's deferred half), and the **live `dbt`-step DAG dispatch** (the `dbt` step type — pipelines/runtime). **Non-blocking reviewer suggestions for a later unit:** surface the subprocess timeout in the captured logs (the watchdog fires silently today); the `--full-refresh`-on-`test`/`snapshot` guard + the `target` option-guard were applied this unit; tests are not mypy-strict-clean.

- **pipelines — the deterministic composition core (Increment 3).** Landed 2026-06-24: the **first unit** of the pipelines capability — the runtime-independent half that needs no live scheduler/worker — built **in units like the engineers**, all gates green (ruff/mypy --strict) at **1796 pytest** (5 skipped, 0 failed), adversarial multi-agent review CLEAN after **one fix iteration**. **`core/config/pipeline_schema.py`** — the pydantic model set (`PipelineMeta`/`SeedSchedule`/`FailureMode`/`DltStepConfig`/`DbtStepConfig`/`SqlStepConfig` as a `type`-discriminated union, `Pipeline`) + `load_pipeline(path, *, components, paths)` validation: unique ids, `depends_on` refs/cycles, `croniter` cron, the retired-`artifact`-key rejection (migration-pointing), `paused`/`enabled` rejection on the seed (`extra="forbid"`), component-name resolvability via the shipped locator, **and step-type/component-type agreement** (a dbt step can't reference a dlt component). New **`src/carve/runtime/`** package (co-owned — Increment 4 runtime extends it additively): **`pipeline_dag.py`** (`PipelineDAG` — DFS cycle detection, Kahn topo order, `ready_steps`, `downstream_of` for `skip_downstream` + `--resume`); **`failure_modes.py`** (the 5-mode table over a `RunState` + `apply_failure_mode`/`derive_run_status` succeeded/failed/`partial` derivation); **`jinja_context.py`** (sandboxed `SandboxedEnvironment` + `StrictUndefined`; the `env` allow-list is **EMPTY by design** — no secrets); **`step_executor.py`** (the forward-declared `StepExecutor` Protocol + `StepExecutorRegistry` + a pipelines-local `StepResult` — the seam Unit 2 / Increment-4 runtime own, deliberately distinct from M1's `core/steps/base.StepResult` vocabulary); **`execute_pipeline.py`** (the async DAG walk; runtime-independent via an injected no-op `StepSink`, a slot-capped wave barrier, an injectable retry-backoff sleeper); **`component_resolution.py`** (typed `resolve_dlt_component`/`resolve_dbt_component` wrappers over the locator); **`run_context.py`** (a frozen `PipelineRun` value object keeping `execute_pipeline` independent of the Increment-4 `Run` row). `pyproject` adds **croniter** + **jinja2**. Tests under `tests/runtime/` + `tests/core/config/`. **The one fix iteration** (1 blocker + 2 majors + 3 minors, all fixed + regression-tested + re-verified CLEAN): (blocker) `DATABASE_URL` removed from the Jinja `env` allow-list — a sandboxed template could read the DB password, so the env namespace is now empty; (major) a **raising** `StepExecutor` (incl. the registry-lookup unregistered-type case) is now caught and degraded to a `failed` `StepResult` that flows through the failure-mode machine instead of crashing the run + discarding concurrent siblings via `asyncio.gather`; (major) `load_pipeline` now enforces step-type/component-type agreement; (minors) warn/continue/skip-failed steps are recorded into the Jinja `steps` namespace with their real status, and the wave-barrier parallelism is documented as a deliberate Unit-1 simplification. Capability spec sanity-checked; **two MINOR inline fixes** (the misleading `DATABASE_URL` env example replaced with the empty-allow-list reality + callout; the step-type/component-type agreement validation recorded) — otherwise the design body (the TOML schema, the DAG, the 5 failure modes, the Jinja sandbox, component resolution) already matches; left phase-free as a forward design doc on its first slice. **Remaining pipelines units** (resumable via `/build-spec pipelines`): **Unit 3** — the `pipeline-engineer` agent (`builtin/pipeline-engineer.md`) + `pipeline_inspect`/`list_components` skills + the `carve pipelines list/show/validate/diff` + `carve component`/`carve components show` + `carve schedule reseed` CLI + the verify-by-validate loop. **DEFERRED to runtime (Increment 4):** the scheduler/job-queue/worker pool, the `schedules` table as live data + `carve schedule` mutation, the definition+seed reconciler, `step_runs` rows + `step.*` events. The `StepExecutor`/`StepExecutorRegistry` seam + the no-op `StepSink` + `run_context.PipelineRun` are **forward-declared for runtime to assume ownership** (relocate/extend, signature-stable). **Review notes for runtime:** (a) a raising **INJECTED** `StepSink` (`sink.step_started`/`step_finished`) can still escape `asyncio.gather` and discard concurrent siblings — the runtime should **exception-isolate the real sink** when it wires it (the no-op default is safe); (b) the wave-barrier loop may be replaced with a streaming `asyncio.wait(FIRST_COMPLETED)` slot model for tighter slot reuse, signature-stable.

- **pipelines — Unit 2: the three concrete step executors (Increment 3).** Landed 2026-06-24: the `dlt`/`dbt`/`sql` step executors (implementing the Unit-1 `StepExecutor` Protocol over the **shipped** backends) + the `resolve_connection` connection factory + `build_step_executor_registry`, all gates green (ruff/mypy --strict) at **1849 pytest** (5 skipped, 0 failed), adversarial multi-agent review CLEAN after **2 fix iterations** (the 2nd reproduce-tested CLEAN). New `src/carve/runtime/step_types/{dlt,dbt,sql,connections,registry}.py` (tests under `tests/runtime/step_types/`). **Mechanisms:** the **dlt** executor runs the component's Python **entrypoint** via the shipped `Subprocess` primitive (there is **no** `dlt pipeline run` CLI) + parses the on-disk **load package** for the verdict (`loaded` + no `failed_jobs`, exit-code-gated) and `outputs` (`{tables, schema_changes, failed_jobs}` — per-resource `rows_loaded` lives only in the in-process trace, not the persisted package, so it's omitted); the **dbt** executor calls `LocalDbtBackend.run(DbtCommand)` → maps the backend-uniform `DbtRunResult` → `StepResult` (timeout 3600 passed through — the 1800-vs-3600 question is RESOLVED, not a defect); the **sql** executor opens a connection **by name** via the new `resolve_connection` factory, path-confines the `.sql` read, renders the file body through the sandboxed Jinja `{steps, run, env}`(+`vars`) namespace, runs it single-transaction, and captures the first-N JSON-safe rows. With this unit `execute_pipeline` now runs a **real dlt→dbt→sql pipeline creds-free over DuckDB**. **The 2 fix iterations** (all regression-tested, the 2nd reproduce-tested CLEAN): an earlier Jinja-sandbox DB-password class (fixed in Unit 1, re-confirmed), then a **cross-run stale-package false-green** (a persistent `DLT_DATA_DIR` re-run that wrote nothing new would read a PRIOR run's `loaded` package and report `succeeded` — now gated on `load_id >= run.started_at`, fail-loud on stale-only) and a **timeout-path connection leak** (a `wait_for` timeout closing a still-`None` lazily-opened session, then the orphaned worker opening it post-close — now the worker owns open+run+close in its own `finally`, leak-free). Capability spec sanity-checked; **three MINOR inline fixes** (the dlt-executor prose/pseudocode now run the entrypoint + read the load package, not the nonexistent `dlt pipeline run` CLI / `state.json` `rows_loaded`; the cross-step Jinja TOML example now references a real output key `tables` not the would-StrictUndefined `rows_loaded`; the sql-executor section now names the `resolve_connection` factory + the `{steps, run, env}`(+`vars`) file-body render per FIX-S5) — otherwise the design body already matches; left phase-free. **DEFERRED/forward notes:** **(1)** the **dlt component-run entrypoint convention is a dlt-engineer ↔ pipelines reconciliation item** — the executor's default tries `scripts/__init__.py`→`pipeline.py`→`__init__.py`→`main.py` (works for the reference HN pack, whose `scripts/__init__.py` has a `__main__` guard) but a carve-authored bare-source `el/<name>/__init__.py` with NO `__main__` guard would be `python __init__.py`-imported and exit **without running a load** (a false success); the dlt-engineer must author a runnable entrypoint (a `__main__` guard / a `run()` the executor invokes) or the executor + convention must be jointly specified. **(2)** Live-wiring (connect/runtime): destination-cred injection + venv materialization for the dlt step are deferred (the default uses `Subprocess` directly, not `LocalVenvRunner`, so `_snowflake_env` layering + `requirements.txt` are ignored — fine for creds-free DuckDB, a real warehouse needs connect); connection **pooling** for the sql step (`SnowflakePool`) is a runtime concern (the executor opens+closes per step — leak-safe, not pooled); per-step dlt `write_disposition`/`resource_select` overrides now **fail loud** (honoring them is deferred to live dlt wiring). **(3)** sql per-step **timeout** is enforced via `asyncio.wait_for` (→ failed), but a `to_thread` worker can't be force-cancelled — the runaway query keeps running until it finishes (the connection is closed by the worker, leak-free); a driver-side query timeout is the deeper Increment-4 fix. **(4)** Test-hygiene follow-up: the real-dlt default-mechanism test should pin `DLT_DATA_DIR`/`pipelines_dir` so dlt doesn't write a stray `*.duckdb` into cwd.

- **pipelines — Unit 3: the pipeline-engineer agent + skills + CLI + verify-by-validate (Increment 3) — COMPLETES the pipelines capability's buildable slice.** Landed 2026-06-25: the **last** pipelines unit, all gates green (ruff/mypy --strict) at **1928 pytest** (5 skipped, 0 failed), adversarial review CLEAN after **one fix iteration** (1 blocker + 2 majors + 2 minors fixed + regression-tested). **`core/agents/builtin/pipeline-engineer.md`** — the declarative build-mode agent (frontmatter: `tools: [edit, grep, pipeline_inspect, list_components, list_dbt_models, sql, web_fetch, "mcp:*"]`, `allowed_paths: ["pipelines/**"]`, `max_mode: build`, 4 classifications `compose_pipeline`/`modify_pipeline_steps`/`seed_schedule`/`schedule_existing_component`; `model:` **omitted** → `default_model`; **NO `bash`** — the prompt was reframed so the **HARNESS** runs the verify-by-validate loop on the agent's behalf, the agent only `edit`s the TOML and reads the structured result back). **`runtime/skills/{pipeline_inspect,list_components,list_dbt_models}.py`** — the 3 callable Tools (`pipeline_inspect` reads a parsed `pipelines/<name>.toml`, path-confined to `pipelines/**`; `list_components` enumerates `el/<name>/` dirs + `[components.*]` block names+types; `list_dbt_models` aliases the shipped `make_dbt_manifest_tool` op=`list_models`). **`runtime/pipeline_verify.py`** — the verify-by-validate vertical (`parse_pipeline_validate` ParseFn + `make_pipeline_verification_loop` over the gated `bash` + `run_pipeline_dev_run` over the shipped `execute_pipeline`; **single-execution-path invariant preserved** — it only *parses* validate output + *drives* the in-process dev DAG walk, opens no new exec path). **CLI:** `cli/commands/pipelines.py` (MODIFY → sub-group `validate`/`list`/`show`/`diff`); `cli/commands/component/{__init__,graduation}.py` (`carve component` graduation via comment-preserving `tomlkit` + `carve components show`); `cli/commands/schedule/__init__.py` (`schedule reseed` deferred stub); `cli/main.py`. **A harness-policy change:** `core/agents/permissions/{bash_gate,policy}.py` — added `carve` to `_SUBCOMMAND_PROGRAMS`, `"carve pipelines validate"` to the read-bash allowlist, and `DANGEROUS_BASH_FLAGS["carve"]={"--project-dir"}` so the engineer's harness-run verify loop can ride `carve pipelines validate` through the gate (it was DEAD without this; review confirmed the scoping is safe — ONLY `carve pipelines validate` is allowed, every other `carve` subcommand denies). **The one fix iteration** (all regression-tested): (BLOCKER) graduating a *dlt* component backfilled its name into omitting *dbt* steps → broke step-type/component-type validation — now guarded on `ComponentType.DBT` (`test_graduate_dlt_component_does_not_backfill_dbt_steps` + the positive dbt case); (major, security) `carve pipelines validate <name>`/`show <name>` path-traversal — now agent-reachable via the allowlist — path-confined to `pipelines/**` mirroring the `pipeline_inspect` guard (`test_validate_path_traversal_name_exits_two`, `test_show_path_traversal_name_exits_two`); (major) graduation crashed on a malformed pipeline TOML leaving `carve.toml` partially mutated → now skips malformed files (`test_graduate_skips_malformed_pipeline_file`); (minors) a `..`-laden component name is rejected (`test_component_name_with_traversal_exits_two_and_leaves_carve_toml`); a duplicate `load_config` removed. Capability spec sanity-checked; **two MINOR inline fixes** (the `pipeline-engineer.md` frontmatter example reconciled to the shipped file — `model:` omitted, `max_mode: build` shown, `"mcp:*"` in `tools:` — the same editorial reconciliation the dlt/dbt-engineer agents took; the Tests-section dlt-executor bullet de-staled from `state.json` parsing to load-package parsing, completing the Unit-2 prose reconciliation) — otherwise the §"Pipeline engineer subagent" (NOT-granted-bash, verifies "through the harness verification primitive"), the §"CLI: carve pipelines/component/schedule reseed", and the §"Verification loop" already match the shipped reframe (left phase-free). **This COMPLETES the pipelines capability's buildable slice (Units 1+2+3 done):** the TOML schema + `load_pipeline` + `PipelineDAG` + 5 failure modes + sandboxed Jinja + `execute_pipeline` (Unit 1), the 3 concrete `dlt`/`dbt`/`sql` step executors (Unit 2), and the pipeline-engineer agent + 3 skills + `carve pipelines validate/list/show` + `carve component(s)` graduation/show + the verify-by-validate vertical (Unit 3) have all landed. **DEFERRED — runtime/Increment-4-gated STUBS:** `carve schedule reseed` (errors "not available yet"), the `pipelines list/show` run-history columns, and `carve pipelines diff` are gated on the `schedules`/`runs`/`step_runs` tables (the runtime's, Increment 4); they render a placeholder/note and exit cleanly rather than query a table that doesn't exist. **DEFERRED — cross-cutting wiring (blocked on the plan-build classifier):** the LIVE orchestrator goal-routing that delegates a composition goal to the pipeline-engineer and rides the verify loop is the plan-build-classifier wiring (the loop is testable in isolation with an injected delegate, which the suite exercises). **STILL OWED:** the dlt-engineer ↔ pipelines **entrypoint-convention** reconciliation (from #41 / the Unit-2 forward note (1)) — a carve-authored bare-source `el/<name>/__init__.py` with no `__main__` guard would import-and-exit without running a load (a false success); the dlt-engineer must author a runnable entrypoint or the executor + convention must be jointly specified. **Pre-existing cleanup (note only, NOT this unit):** two merged-unit test files (`tests/core/agents/builtin/test_dlt_reviewers.py`, `tests/core/skills/test_dlt_strategy_packs.py`) were committed not-`ruff format`-clean (CI runs `ruff check`, not `ruff format --check`) — a trivial later-pass cleanup.

- **plan-build — Unit 1: formalization + the config-hash drift gate (Increment 3).** Landed 2026-06-25: the **first** plan-build unit (the safety-net + entity-formalization slice that needs no live delegation), all gates green (ruff/mypy --strict) at **1955 pytest** (5 skipped, 0 failed), both reviewers PASS with **no Must-Fix**. **`cli/orchestrator/builder.py`** — `build_plan` now runs three gates in a deliberate order: **(1) the config-hash drift gate** — `plan_row.config_hash != config.config_hash` raises `ConfigDriftError` → `cli/commands/build.py` maps to exit **3** + a "re-plan against current config" message; it is ordered **BEFORE** the `--force`/phase gate so `--force` overrides "already built" but **cannot** bypass drift; **(2) expiry** — a plan past `expires_at` raises `PlanExpiredError` → exit **2** (injectable `now` for deterministic tests); **(3) idempotent rebuild** — re-building an already-built plan against unchanged config with its recorded manifest files still present is a **no-op** (returns the existing `current_build_id`, creates no new Build row, never runs the agent); a *missing* manifest file declines the no-op and falls through to the phase gate (still needs `--force`) — a safe narrowing. **`cli/orchestrator/cost_rollup.py` (new)** — the pure cost/runtime synthesis seam: `roll_up_cost` sums each `DelegationResult.usage`/`cost_usd` into a `CostRollup`; `compose_runtime_estimate` builds a `RuntimeEstimate` (first-run/subsequent durations) from the engineers' `expected_outputs`, degrading gracefully when a hint is absent; **NO warehouse-dollar field** (UC1 — `CostRollup` has no field that could carry one). Wired through `planner.py` as a single-element rollup today (the one-loop plan flow feeds the same seam; numbers unchanged); Unit 2's live per-subagent delegation feeds the multi-element rollup. **`cli/commands/plan_and_build.py` (new)** — the thin `plan-and-build` one-shot verb (registered in `cli/main.py`). **`core/config/pipeline_schema.py`** — a circular-import fix: the `component_locator` import is **deferred into the function body** (importing at module top created a `core.config ↔ integrations.component_locator` cycle), with an isolated-import regression test (`tests/core/config/test_pipeline_schema_import_order.py`). `classification=None` / `resolve_agent_or_fallback` are **UNTOUCHED** — the live agent wiring is Unit 2. Tests: `tests/cli/orchestrator/{test_drift_gate,test_plan_expiry,test_build_idempotent,test_cost_rollup}.py`, `tests/cli/commands/test_build_exit_codes.py`, + the `builder`/`planner` test MODIFYs. **Capability spec sanity-checked; MINOR inline drift only** — the entity/cost/drift/expiry/idempotency/verbs design body already matches what shipped; the one honest gap is the **`post_build` emit** (see the ownership decision below), recorded as inline callouts in `plan-build.md` (§"The Build entity" + Acceptance + Tests) and `extensibility.md` (the `post_build` emission-point line). **`post_build` emitter — cross-spec ownership decision (reviewers flagged the tension).** plan-build's Acceptance says "build … emits `post_build`", but Unit 1's builder does **NOT** emit it; [extensibility](./capabilities/extensibility.md) shipped only the *subscription* seam — `core/hooks/events.py` keeps `POST_BUILD` in `DEFERRED_EMITTER_EVENTS`, so `HookRegistry.emit` **loudly raises `DeferredEmitterEvent`** (an honest seam, not a silent drop), and the extensibility delivery spec assigned the emitter to "build/pipelines = Incr 3". **Decision: the `post_build` emitter is owned by plan-build's builder (the `carve build` verb lives there, not in pipelines), and is wired in plan-build Unit 2** — a small follow-up against the existing seam: lift `POST_BUILD` out of `DEFERRED_EMITTER_EVENTS` and have `build_plan` fire it after materialization (the `Build`/`current_build_id` are already recorded there). Until then "build emits `post_build`" is honest only as the *seam not yet fired* — the inline callouts make both specs say so. **Unit 2 (the live multi-engine synthesis + orchestrator wiring) is the remaining plan-build unit** (resumable via `/build-spec plan-build`): the **goal classifier** + `extra_tools` assembly + **live `SubagentRunner` delegation** + the **live review fan-out** (over the shipped `review_fan_out.py` driver) + **synthesis** of the per-subagent diffs/costs into one reviewable Plan (feeding the multi-element `roll_up_cost`), plus firing `post_build`. **Unit 2 is the keystone that unblocks live agent routing across the whole corpus** (every "deferred — blocked on the plan-build classifier" wiring note in this Current state) **and the M1 `extract_load` retirement** (the dlt-engineer's last unit). **Non-blocking suggestions recorded** (none gate the unit): the idempotency check could compare per-file **content hashes** rather than mere file existence (a manually-edited manifest file currently still satisfies the no-op); add a CLI-level `plan-and-build` **happy-path** integration test; and wire the **`post_build` emitter** per the ownership decision above (Unit 2).

**Increment 1 is complete** — layout, harness, extensibility, model-auth (as Increment 1b), and packaging are all shipped. **Increment 2 is complete** — the `init`, `memory`, and `sql` lean cores have all landed, with their heavier parts deferred to tracked issues. **Increment 3 is in progress** — the tool binder (the foundational unblock for declarative engineers), the dlt-engineer (substrate → callable skills → the agent + verification runner → the reviewers + review fan-out; only M1 `extract_load` retirement remains), the **dbt-engineer** (substrate: the `dbt_manifest` reader + the `parse_dbt_run` verify bridge → the **agent + author→run→verify vertical + dbt-qa reviewer**, riding the now-shipped `LocalDbtBackend`; only the **brownfield convention-inference engine + greenfield scaffolding** unit remains), the **dbt-execution local-backend slice (the `DbtBackend` interface + `DbtRunResult` + the `local` dbt-core subprocess backend + engine resolve/pin)**, and the **pipelines capability — now COMPLETE through its buildable slice** (Unit 1: the deterministic composition core — the TOML schema + `load_pipeline` + `PipelineDAG` + the 5 failure modes + the sandboxed Jinja context + the `StepExecutor` seam + `execute_pipeline` + component resolution; Unit 2: the three concrete `dlt`/`dbt`/`sql` step executors — `execute_pipeline` runs a real dlt→dbt→sql pipeline creds-free over DuckDB; **Unit 3 (the last): the pipeline-engineer agent + `pipeline_inspect`/`list_components`/`list_dbt_models` skills + `carve pipelines validate/list/show` + `carve component(s)` graduation/show + the verify-by-validate vertical + the `carve pipelines validate` bash-allowlist entry**) have landed. **With pipelines complete and plan-build Unit 1 (the config-hash drift gate + expiry + idempotency + the cost-rollup seam + `plan-and-build` + the circular-import fix) landed, the remaining Increment-3 work is:** **plan-build Unit 2** — the **live multi-engine synthesis + orchestrator wiring** (the goal classifier + `extra_tools` assembly + live `SubagentRunner` delegation + live review fan-out + synthesis, plus firing `post_build`), **the keystone that unblocks live agent routing across the corpus *and* the M1 `extract_load` retirement**; the dbt-engineer's **brownfield convention-inference + greenfield-scaffolding** unit (the same convention-inference blocker as init #9 / memory `refresh`); and **connect** (AI-driven engine-install/pin + warehouse/source connect — net-new, unbuilt). **dbt-execution's first slice unblocked the dbt-engineer's verify-by-execution loop** (the live `dbt build`/`test` its `verify_bridge`/`runner` ride exists, now wired through `LocalDbtBackend`); dbt-execution's **managed** backends + worker placement + lazy engine install + the live `dbt`-step DAG dispatch + a `dbt source freshness` command remain deferred. **The cross-cutting blocker across the remaining work is the live orchestrator wiring — goal classification + the live execution / review-fan-out / pipeline-composition-delegation paths — and that work *is* plan-build Unit 2** (every "deferred — blocked on the plan-build classifier" note above resolves when Unit 2 lands). Pipelines' own runtime parts (the scheduler/worker/reconciler, the `schedules`/`runs`/`step_runs` tables, `carve schedule reseed`/`pipelines diff`/run-history) are Increment-4 runtime by design (deferred stubs ship today), and the dlt-engineer ↔ pipelines entrypoint-convention reconciliation (#41) is still owed; then runtime (Increment 4). The full corpus is internally consistent under the control-plane + AI-harness model.

---

## Increment 0 — Baseline: formalize the shipped state store ✅ *(done 2026-06-18)*

**Goal.** Reconcile the shipped Postgres state store with its spec — the foundation every increment plans against.

**In scope**
- Postgres state store — [state-store](./capabilities/state-store.md) *(landed; spec reconciled — verified green against a live Postgres testcontainer)*

> **Re-slotted.** Two capabilities first bucketed here moved out. **plan-build → Increment 3** (its plan-synthesis rolls up the engineers' diffs/costs; formalizing it against the rebuilt foundation — not the soon-to-be-replaced M1 shape — is where it's real). **model-auth → Increment 1b** — and *not* as a formalize: only its `ANTHROPIC_API_KEY` path shipped in M1.1; the OAuth flow and the single credential-precedence resolver were planned (M1.1-02) but never built, so picking it up is a **net-new build**, not reconciliation of shipped code.

**Depends on.** Current state only.

**Delta.** Verify/MODIFY against shipped code (the M1 fixture sweep + the three unit tests already landed). No new code — the spec was made honest (Status → Landed).

**Exit criteria.** ✅ The state-store spec matches the code; the full state/migration test surface is green against Postgres.

---

## Increment 1 — Foundation: control-plane layout + the AI harness ✅ *(complete 2026-06-21)*

**Goal.** The structural + AI substrate everything runs on: a control-plane `carve.toml` that references components by name, and the Claude-Code-style harness (subagents, terminal tools, permission gate, verify-by-execution) with declarative extensibility.

**In scope**
- OSS packaging: bundled docker-compose Postgres + external-Postgres option — [packaging](./capabilities/packaging.md)
- Control-plane flat layout: `carve.toml` `[components.<name>]`, the component locator, repo topology, simple-mode convention discovery, the workspace cache — [layout](./capabilities/layout.md)
- **The agent harness** — subagent `delegate`, terminal tools (edit/bash/grep/web), the permission gate (modes + `allowed_paths` + bash sandbox + secret-deny), verify-by-execution, interrupt/TODO/compaction — [harness](./capabilities/harness.md)
- **Extensibility** — declarative agents (`carve/agents/*.md`), skill packs (`SKILL.md`), hooks (`hooks.toml`), MCP both directions, runtime grant attenuation — [extensibility](./capabilities/extensibility.md)

> *Model auth re-slotted to **Increment 1b** below — only its API-key path shipped in M1.1, so the OAuth + precedence-consolidation work is a net-new build, not a formalize.*

**Depends on.** Increment 0 (the state store) + M1/M1.1 (the agent loop the harness wraps; the M1.1 API-key model auth the harness already uses).

**Delta.** 15 *wraps* the M1 agent loop (adds delegation, the gate, context management); it does not replace it. 16 is net-new. 03 introduces `carve.toml` as control-plane config (supersedes the M1 project-shaped config) + the locator (net-new).

**Exit criteria.** A `carve.toml` with `[components.<name>]` resolves names to code (simple + multi mode); an agent runs under the permission gate with terminal tools and can `delegate` to a subagent that verifies by execution; a user-authored `carve/agents/*.md` overrides a built-in, attenuated to its mode. *(Model auth → Increment 1b.)*

---

## Increment 1b — Model auth: OAuth + credential-precedence consolidation ✅ *(done 2026-06-21)*

**Goal.** Make "how Carve authenticates to its model provider" an owned subsystem: one credential-precedence resolver, the `auth_mode` + model-tiers schema in `models.toml`, the OSS-vs-hosted seam, and the Claude-subscription OAuth path (`carve auth login`) — on top of the API-key path that already ships.

**In scope**
- Model auth — `auth_mode` + model `tiers` on `ModelsConfig` (+ `default_model` fixed to a current id, with the pricing table updated to match); a single `client_factory` / precedence resolver collapsing the four `anthropic.Anthropic(api_key=…)` call sites into one place; SDK-native OAuth via `auth_token=` + the `oauth-2025-04-20` header (sourced from `ANTHROPIC_AUTH_TOKEN` / `CLAUDE_CODE_OAUTH_TOKEN`); `carve auth status` + `carve auth login` (a thin wrapper over `claude setup-token`); the OSS-vs-hosted split — [model-auth](./capabilities/model-auth.md)

**Depends on.** Increment 1 (harness — the credential's consumer; layout — `models.toml`'s config-bundle home).

**Delta.** *Net-new, not a formalize.* Only the `ANTHROPIC_API_KEY` path shipped in M1.1. **MODIFY:** `ModelsConfig` (add `auth_mode`/`tiers`, fix `default_model`), the four client-construction sites (→ one `client_factory`), the pricing table (add the current model ids so the new default is priced). **CREATE:** `client_factory` (the precedence resolver + SDK-native OAuth bearer wiring) and the `carve auth` CLI. Carve builds **no** browser flow or token store.

**Investigation (resolved).** The M1.1-02 questions are answered: the `anthropic` SDK authenticates a subscription OAuth bearer natively via `auth_token=` (Bearer) + the `anthropic-beta: oauth-2025-04-20` header — same `messages.create` surface, no adapter, identical usage shape — so **no** `claude-agent-sdk` dependency and **no** Carve-owned browser flow / token store / refresh are needed; tokens are minted by `claude setup-token` / `ant auth login`. `api_key` stays the default.

**Exit criteria.** The agent layer authenticates via **either** `ANTHROPIC_API_KEY` **or** a Claude-subscription OAuth bearer through one precedence resolver (`client_factory`), sending exactly one credential and a clear error when neither is present; the OAuth path attaches the `oauth-2025-04-20` header; `carve auth login` wraps `claude setup-token` and `carve auth status` reports the active mode without leaking secrets; `models.toml` carries `auth_mode` + `tiers` and `default_model` is a current, priced id; the OSS-vs-hosted split is explicit.

---

## Increment 2 — Bootstrap & SQL: a scaffolded project + the dialect-aware tool

**Goal.** Scaffold a real project (greenfield or brownfield) with project memory, and stand up the dialect-aware SQL tool every engineer rides on.

**In scope**
- **`carve init`** — greenfield/brownfield across the Postgres × dbt × dlt × memory axes; renders the control-plane `carve.toml`; convention inference — [init](./capabilities/init.md). *Lean core landed 2026-06-22 (see Current state); the deferred parts — convention inference, interactive prompts, `--migrate-from-targets`, dbt-engine detection/eager-install, auth-token bootstrap, getting-started docs — remain in scope here.*
- **Project memory** — `conventions.md` / `standards.md` / `decisions.md`, sidecars, `carve memory` surface — [memory](./capabilities/memory.md)
- The dialect-aware **SQL tool layer** — sqlglot validate/transpile, per-dialect introspection, role-gated exec (Snowflake + DuckDB first-class) + a thin SQL specialist — [sql](./capabilities/sql.md)

**Depends on.** Increment 1 (layout, harness, extensibility) + Increment 0 (the state store).

**Delta.** 05 *rewrites* the M1.1 init around the control-plane `carve.toml` + the four axes. 06 builds on 05's scaffolded memory files. 18 *generalizes* the M1 Snowflake-only `run_snowflake_query` + catalog skills into a dialect-aware layer (preserves the connector).

**Exit criteria.** `carve init` produces a working project (bundled or external Postgres) with memory scaffolding; brownfield init infers conventions and writes no `[components.*]` blocks in simple mode; the `sql` tool introspects a live warehouse on the read role.

---

## Increment 3 — Components: the AI authors, runs & composes dlt **and** dbt

**Goal.** The AI authors **both** dlt and dbt components (co-equal), runs them, provisions backends on demand, and composes components by name into a runnable pipeline DAG.

**In scope**
- The **DLT engineer** subagent (authors/runs dlt; native/REST/curated-library/MCP paths) + dlt-qa / dlt-security reviewers — [dlt-engineer](./capabilities/dlt-engineer.md)
- The **dbt engineer** subagent — authors/modifies dbt models, tests, sources; verifies via `dbt build`/`test`; + a dbt-qa reviewer — [dbt-engineer](./capabilities/dbt-engineer.md)
- **dbt execution backends** — local (bundled Fusion/dbt-core, or the team's own dbt) + managed (snowflake-native, dbt Cloud, remote), behind one step interface — [dbt-execution](./capabilities/dbt-execution.md)
- **connect** — AI-driven on-demand provisioning: engine install + pin, warehouse/source connect — [connect](./capabilities/connect.md)
- **Multi-step pipeline** composition: `pipelines/<name>.toml`, the step DAG executor (dlt/dbt/sql), `[seed_schedule]`, component-by-name, the definition reconciler, the **pipeline engineer**, `carve component(s)` graduation — [pipelines](./capabilities/pipelines.md)
- **Plan / build** *(formalize M1.1-shipped + complete)* — the Plan/Build entities + `plan`/`build`/`plan-and-build` verbs + `--refine` (shipped), the config-hash drift gate, and the plan synthesis that now rolls up the engineers' verified diffs/costs — [plan-build](./capabilities/plan-build.md)

**Depends on.** Increment 2 (init/memory/sql) + Increment 1 (harness, layout, extensibility).

**Delta.** 04 *replaces/generalizes* the M1 EL agent as a declarative subagent. **dbt-engineer is net-new — the exact parallel to the DLT engineer, co-equal from the start.** dbt-execution is net-new: it implements the `dbt` step against the StepExecutor protocol (the runtime's scheduler + worker-placement *dispatch* it in Increment 4). connect + dbt-execution are co-designed (the bundled-engine provisioning seam). 08 is net-new (the reconciler creates the `pipelines`/`schedules` tables). plan-build formalizes its M1.1-shipped lifecycle core + adds the config-hash drift check + the synthesis rollup, now that the dlt/dbt engineers produce the diffs/costs it composes.

**Exit criteria.** `carve plan "ingest Stripe, then stage it with dbt"` → the **DLT and dbt engineers** author + verify their components (`dlt pipeline run`, `dbt build`/`test`); `carve build` materializes them + a `pipelines/<name>.toml` referencing them by name; `carve pipelines validate` passes; first dbt use provisions + pins the engine via `connect`; `carve plan` rolls up exact LLM cost + a runtime estimate from the engineers' diffs, and `carve build` refuses a drifted plan (exit 3).

---

## Increment 4 — Runtime & telemetry: schedule, run, record

**Goal.** Schedule and run composed pipelines end-to-end on cron, with telemetry.

**In scope**
- The **runtime** — scheduler (reads the `schedules` table), Postgres job queue (optimistic claim), worker pool, heartbeats, reaper, archiver; the live `schedules` table + `carve schedule` mutation surface + `schedule_changes` audit; dispatches dlt/dbt/sql steps (incl. worker placement for dbt-execution's local backend) — [runtime](./capabilities/runtime.md)
- **Observability** — agent/run/step/skill telemetry tables, `carve metrics` rollups (token→$, run success/failure, per-agent usage), OpenTelemetry/OTLP export — [observability](./capabilities/observability.md)

**Depends on.** Increment 3 (08's pipeline definitions + reconciler the scheduler reads; dbt-execution's steps the runtime dispatches; the engineers whose runs observability records).

**Delta.** 07 *wraps* the M1 `LocalVenvRunner` in a scheduler + queue + worker layer; creates the runtime tables (jobs, workers, archives, events, **schedules**, schedule_changes); completes the worker-placement dispatch for dbt-execution's local backend. observability records over runtime's events + the harness's per-agent-invocation telemetry (the instrumentation hook is wired here); the `/metrics` REST surface lands in Increment 5.

**Exit criteria.** `carve serve` schedules + runs a composed dlt→dbt→sql pipeline on cron; `carve schedule pause/resume/set-cron` changes firing instantly, audited; `carve metrics` rolls up cost / runs / per-agent usage.

---

## Increment 5 — Interfaces & investigation: REST, MCP, UI, ask, lineage, search

**Goal.** Drive Carve programmatically, and investigate the project read-only.

**In scope**
- **REST API** — full CLI-surface coverage, auth, errors, pagination, streaming, webhooks (incl. `/metrics/*` onto observability) — [rest-api](./capabilities/rest-api.md)
- **MCP server** — auto-generated from REST; stdio + WebSocket — [mcp-server](./capabilities/mcp-server.md)
- **Static HTML UI** — regenerated per run; `carve docs serve` — [ui](./capabilities/ui.md)
- **The explorer (`ask`)** — read-only investigative subagent; citations — [ask](./capabilities/ask.md)
- **Lineage by investigation** — the `dlt_schema` reader skill; the explorer answers lineage via dbt manifest + dlt schema + code (no Carve store) — [lineage](./capabilities/lineage.md)
- **Semantic search** — the embedding index + `semantic_search` skill + `carve embeddings rebuild` — the fuzzy retrieval layer atop the deterministic ones — [semantic-search](./capabilities/semantic-search.md)

**Depends on.** Increment 4 (surfaces to expose) + Increment 3 (the dbt manifest lineage reads). 12/19/semantic-search need the harness (1) + SQL tool (2); 10/11 need 09; semantic-search needs ask + lineage.

**Delta.** Largely net-new surfaces over the increments 1–4 substrate. 12 subsumes the old ask-only guardrail into the `read_only` mode. 19 adds one skill (`dlt_schema`) + explorer guidance — no graph. semantic-search adds the embedding index + skill + rebuild command.

**Exit criteria.** Every CLI action has a REST + MCP equivalent (parity); `carve ask "where does X come from?"` returns a cited answer via investigation; `carve ask` resolves a fuzzy concept ("churn metrics") via semantic search; the static UI shows run history + per-run logs.

---

## Increment 6 — Deploy & recovery

**Goal.** Promote built code to prod, and auto-diagnose failures.

**In scope**
- **Deploy** — `carve deploy <pipeline>` configurable handoff (files/commit/push/pr, default pr); cross-repo linked PRs; pre-flight drift — [deploy](./capabilities/deploy.md)
- **Recovery engineer** — diagnose-then-delegate on retries-exhausted `run.failed`; the `Investigation` entity; auto-pause/resume gated by pause origin — [recovery](./capabilities/recovery.md)

**Depends on.** Increment 3 (deploy targets) + Increment 4 (07 `run.failed`, schedules auto-pause). 17 needs the harness/delegation (1) + the engineers it delegates to (3) + deploy (this increment, for the resolving-deploy → auto-resume link).

**Delta.** 14 *retires* the `carve el deploy` DDL-apply path; net-new handoff/linked-PR machinery + the `deploys` table. 17 reuses the M1 recovery POC's reconcilable parts; net-new `investigations` table + the diagnose-then-delegate flow (delegating dlt/dbt/sql fixes to the engineers).

**Exit criteria.** `carve deploy` opens a (linked) PR by default, each handoff depth working; a retries-exhausted failure produces a grounded `Investigation` + a reviewable fix Plan, auto-pauses the schedule, and the resolving deploy auto-resumes it (unless a human paused it).

---

## Increment 7 — Reference & initial release

**Goal.** Correct reference docs and tag the initial release — shipping **all 26 capabilities**: the full intent → plan → build → run → deploy → schedule loop for dlt **and** dbt.

**In scope**
- **Reference docs** — cli-reference / config-schema / glossary / governance kept in lock-step via completeness tests — [reference-docs](./capabilities/reference-docs.md) *(content rewritten 2026-06; this increment adds the completeness tests against built code)*
- **Release** — tag the initial release (the semver version is chosen at release time).

**Depends on.** Everything (reference derives from the built surface).

**Delta.** The reference content is already regenerated to the current model; this increment adds the build-time completeness tests (every Typer command in cli-reference; every init-scaffolded file in config-schema) and pins the few **planned** CLI commands by giving each an owning slice or cutting it.

**Exit criteria (initial release).** `carve init → plan → build (dlt + dbt) → run → deploy → scheduled-run-on-cron` works end-to-end against a real Snowflake account, and the same loop works via REST and MCP. Completeness tests green.

---

## Sequencing rationale

```
M1 / M1.1
   │
   ▼
Incr 0  state-store                                      (formalize the shipped state store -- done)
   │
   ▼
Incr 1  packaging · layout · harness · extensibility     (control-plane + AI foundation)
   │
   ▼
Incr 1b model-auth                                       (OAuth + precedence consolidation;
                                                          API-key path already shipped --
                                                          net-new build, not a formalize)
   │
   ▼
Incr 2  init · memory · sql                              (scaffold a project + the SQL tool)
   │
   ▼
Incr 3  dlt-engineer · dbt-engineer · dbt-execution ·    (AI authors / runs / composes
        connect · pipelines · plan-build                  dlt AND dbt; + M1.1 plan-build)
   │
   ▼
Incr 4  runtime · observability                          (schedule / run / record)
   │
   ▼
Incr 5  rest-api · mcp-server · ui · ask · lineage ·     (interfaces + investigation)
        semantic-search
   │
   ▼
Incr 6  deploy · recovery
   │
   ▼
Incr 7  reference-docs + initial release tag             (all 26 capabilities)
```

- **Baseline first.** Incr 0 reconciles the shipped state store with its spec. plan-build (Incr 3) formalizes its M1.1-shipped lifecycle core alongside the rebuilt foundation it integrates with, rather than against the soon-to-be-replaced M1 shape. Model auth is **not** a formalize: only its API-key path shipped, so its OAuth + precedence-consolidation work is a net-new slice (Incr 1b).
- **Foundation before components.** The harness + control-plane layout (1) and the scaffold + SQL tool (2) gate everything AI- and component-shaped.
- **dlt and dbt are co-equal components (3).** Both are authored, verified-by-execution, and composed from the start — dbt is *not* deferred. Execution (dbt-execution) + on-demand provisioning (connect) land with them, before the scheduler, so a dbt step can run the moment it's composed.
- **Capability before interface.** Author / run / compose / schedule (3–4) before exposing over REST/MCP/UI + investigation (5).
- **Deploy + recovery after a pipeline can run** (they act on built/running pipelines).
- **Reference + release last** (derives from the built surface) — the initial release ships the whole loop, dlt and dbt alike.

## Backlog (post-release enhancements)

All 26 capabilities are placed in Increments 0–7 above. This is the living queue for work *after* the initial release — genuine enhancements deliberately scoped out of the first loop. They ride the change lifecycle ([`_strategy/2026-06-change-lifecycle.md`](./_strategy/2026-06-change-lifecycle.md)): a new spec or a spec edit, then `/build-spec`; small ones are GitHub issues, larger ones get an increment here.

- **Concurrent subagent fan-out** — the harness runs sequentially in the initial release; concurrency is a later enhancement ([harness](./capabilities/harness.md)).
- **Column-level lineage** — the explorer reads model SQL on demand today; column-level may arrive via the Fusion dbt engine ([lineage](./capabilities/lineage.md), [dbt-execution](./capabilities/dbt-execution.md)).
- **Custom step-type SDK + in-process custom-skill SDK** — built-in step types + MCP/`SKILL.md` skills ship first ([extensibility](./capabilities/extensibility.md)).
- **First-class Postgres / BigQuery / Databricks / SQL Server** — they author/validate/transpile via sqlglot now; introspection hardening to first-class is later ([sql](./capabilities/sql.md)).
- **Multi-LLM providers** (OpenAI / Google) beyond Anthropic ([model-auth](./capabilities/model-auth.md)).
- **Multiple schedules per pipeline**, Salesforce/SaaS CDC sources, an OAuth side-channel browser flow, opt-in auto-deploy for trivial fixes, and further curated-connector-library waves.
